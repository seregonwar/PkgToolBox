class Archiver:
    def __init__(self, id, name, offset, size, extra):
        self.id = id
        self.name = name
        self.offset = offset
        self.size = size
        self.extra = extra

    # Aggiungi altri metodi necessari per la classe Archiver
